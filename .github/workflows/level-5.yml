name: Level 5 - Bowser's Castle

on:
  workflow_dispatch: {}

# TODO - permissions

jobs:
  image:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # TODO - JAR builden (zonder tests)

      # TODO - Docker context "opzetten"

      # TODO - Inloggen op GHCR (alleen op main)

      # TODO - Container image bouwen m.b.v. Docker (alleen op main)

      # TODO - Container image pushen (alleen op main)
